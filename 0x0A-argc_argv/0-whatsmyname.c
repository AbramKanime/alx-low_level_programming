#include <stdio.h>
#include <stdlib.h>
/**
* main - The entry point
* @argc: the arg count
* @argv: the arg vector
*
* Return: 0 (successful). Otherwise 1
*/
int main(__attribute__((unused)) int argc, char *argv[])
{
	int i = 0;

	printf("%s\n", argv[i]);

	return (0);
}
